# Owner: ALL

# ── API ────────────────────────────────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.9

# ── Auth (Jana) ────────────────────────────────────────────────────────────────
fastapi-users[sqlalchemy]==13.0.0
casbin
casbin-sqlalchemy-adapter
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
email-validator==2.1.0

# ── Cache (Tarek) ──────────────────────────────────────────────────────────────
fastapi-cache2  # no [redis] extra — avoids aioredis which breaks on Python 3.12

# ── Database (Tarek) ───────────────────────────────────────────────────────────
sqlalchemy==2.0.25
alembic==1.12.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
pydantic==2.5.3
pydantic-settings==2.1.0

# ── Queue and Infra (Hadi) ─────────────────────────────────────────────────────
rq
redis==5.0.1
minio
hvac
psycopg[binary]
paramiko

# ── Classifier and Inference (Hawraa) ──────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.4
torchvision>=0.19
Pillow

# ── Shared ─────────────────────────────────────────────────────────────────────
python-dotenv==1.0.0
python-json-logger==2.0.7

# ── Testing ────────────────────────────────────────────────────────────────────
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.25.2
aiosqlite==0.20.0

# ── Training only (Colab/Lightning.ai — not used in docker-compose stack) ──────
# datasets<3.0.0
