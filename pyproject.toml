# Owner: HADI
[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["app/db/migrations/"]

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
